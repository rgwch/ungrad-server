## Preparation and compile

### Prerequisites:

NodeJS and NPM

### First time build:

    cd admin-ui
    sudo npm install -g gulp
    sudo npm install -g jspm
    npm install
    jspm install -y

### Development

    gulp watch
